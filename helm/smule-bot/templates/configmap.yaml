{{- if .Values.configMap.create }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "smule-followers.fullname" . }}-config
  labels:
    {{- include "smule-followers.labels" . | nindent 4 }}
data:
  {{- if .Values.env.TZ }}
  TZ: {{ .Values.env.TZ | quote }}
  {{- end }}
{{- end }}
